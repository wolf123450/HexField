export const APP_NAME = 'HexField'

/** Prefix for all localStorage keys owned by this app. */
export const APP_STORAGE_PREFIX = 'hexfield_'

/** localStorage key set once the user has seen the welcome message. */
export const APP_ONBOARDING_KEY = 'hexfield_onboarding_complete'
