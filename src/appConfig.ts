export const APP_NAME = 'GameChat'

/** Prefix for all localStorage keys owned by this app. */
export const APP_STORAGE_PREFIX = 'gamechat_'

/** localStorage key set once the user has seen the welcome message. */
export const APP_ONBOARDING_KEY = 'gamechat_onboarding_complete'
