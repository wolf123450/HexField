# Cross-platform native binaries (rollup, etc.) are handled by
# scripts/cross-platform-deps.mjs which runs as a postinstall hook.
# It detects the current OS and downloads the missing platform binary
# so the same node_modules works from both PowerShell and WSL.
